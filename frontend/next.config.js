/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com",
      "loremflickr.com",
      "picsum.photos",
    ],
  },
};

module.exports = nextConfig;
